import { MarkQueryWordsPipe } from './mark-query-words.pipe';

describe('MarkQueryWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkQueryWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
