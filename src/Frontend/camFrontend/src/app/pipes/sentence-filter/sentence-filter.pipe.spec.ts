import { SentenceFilterPipe } from './sentence-filter.pipe';

describe('SentenceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SentenceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
